GEMINI PROMPTS

1.
I have project assignment to be completed with Problem Statement:

Design and build a “Decision Companion System” that helps a user make better decisions.

The system should assist a user in evaluating options for a real-world decision of their choice.

Your system must:

Accept multiple options

Accept criteria (which may have different weights or importance)

Process and evaluate options against criteria

Provide a ranked recommendation

Explain why a particular recommendation was made
Give me a very detailed idea on how the core design and algoritham to be used in the project should be and the various approaches I can consider on building this project.

2.
Explain clearly how the weigh sum model algoritham can be used to implement this project making sure the constraints like the system must accept multiple options, accepts criteria, process and evaluate options against criteria is met.


3.
Do a simulation of working of this project in a command line interface.

4.
In the simulation provided the score value and the weigh value is provided by the user, shouldn't the score be determined the system itself?

5.
I chose to build a universal decision engine which is ai powered in order to get score values on various options based on ai research. so now  provide me with core design and  architecture on how to implement this project by keeping this conditions not violated

System must:

Accept multiple options
Accept criteria (which may have different weights or importance)
Process and evaluate options against criteria
Provide a ranked recommendation
Explain why a particular recommendation was made
The system should not be a static hard coded comparison.
The user should be able to change inputs and get different outcomes.
Your logic should be explainable (not a black box).

The roles of Ai in this project include:

  1.To research on the various options provided by the user and score them, or search for various option along with the scores when the user has not provided options
  2.The calculated result from the weigh sum model  is passed on to ai for final refinement of output generation with a ranked recommendation  that clearly states the reason why a particular decision was made based on the algorithms implemented and research done by ai models

6.

now i need you to generate a prompt to build this project with just a Command line interface as frontend where the backend is build using with python django and langchain and langraph framework where the llm model used is the llama model from groq cloud api and use the weigh sum model as the core algorithm

7.

Suggest me better approaches than the weigh sum model which overcomes the limitations of weigh sum model

8.

Explain the topsis algoritham clearly with an example that a guy with no prior knowledge about the algorithm would understand

9.

Explain the fuzzy topsis algoritham clearly with an example that a guy with no prior knowledge about the algorithm would understand

10.

Generate the prompt which tells the cursor to change the core algortham to fuzzy topsis algorithm which ensure a more better and accurate performance

11.

Generate a detailed prompt to be given to cursor to switch it from basic command line interface to streamlit frontend

12.

Generate a detailed prompt to change the frontend streamlit where 1. i need each table for criterias which contains scores of different option for particular criterias
2.Asks user about number of criterias and options required based on the number small boxes appear for criterias which should also contain a field for description of criteria which user inputs which is also optional and this descriptions should be passed on to llm to generate the scores on different options based on criteria
3.Along with scores generated by llm it should also show wether the criteria is a cost or benefit critera and the user should also be able to edit the type of criteria
4. Fuzzy normalization for cost and benefit criteria should be implemented differntly based on the TOPSIS fuzzy logic
5.Explanation for the winner option must contain two sections ,one section which tells how the  current option won algorithamically and the other section explaining how the current option won based on the first description user provided

13.

explain how rag implementation in this project would make the project more impressive 

14.

Give me similar ideas where rag can be useful like uploading of resume

15.

Generate a prompt to be given to cursor "1. To implement rag system to upload pdfs and generate scores on different options based on rag data implemented through chroma db 
2. Put an example in goal/description box for users to clearly understand th idea
3.Ask user wheter you are satisfied with criteria classification into cost and benefit and user can change the type of criteria if required
4.Add a button at end which redirects to a seperate differnt webpage Clearly show how topsis fuzzy algoritam works  so a beginner would understand(small and simple example can also be included)and also then explain clearly in terms of the algoritham,how  the option came out to be the winner showing 
the calclulation step by step
5. Add a footer to the webpage which would contain small contact details "Sion Jose
sionjose2004sj@gmail.com
7025807399


16.

I  uploaded these two resumes as rag in my project and asked to pick one for the role of associate software engineer with two options sion jose and vishnu lal t and asked for fuzzy based on criterions communication and technical skills , and it generated fuzzy scores which were same for both options giving the jutification that the communication and technical skills of vishnu lal and sion jose is not known .Give me a system prompt that ensures that the scores generated for the option to take any decisions would be based on rag data and the llm knowledge and no justification like "is not known is given"



17.


Give me a well detaild prompt to be put into cursor in order to avoid cautious alginment give more scores if anything similar to option is mentioned in the pdf uploaded

18.

now i need to remove the general pdf upload process along with description in the first phase and add file upload as optional option for each options user provides and generate scores for criterion based on the pdf uploaded and also provide a typo what adding files does to  an option


19.

1. Pre-LLM annotation: _annotate_rag_context() scans each RAG section
   for criterion keyword matches and injects visible ">>> KEYWORD MATCH"
   markers so the LLM cannot overlook evidence.

2. Stem matching and tiered guardrail: _criterion_terms() now generates
   truncated stems (first 6 chars of 6+ char words) for broader matching
   (e.g. "commun" catches "communicated", "communicating"). The
   deterministic guardrail uses tiered boosting — strong evidence (2+
   term matches) gets floors of l>=7, m>=8, u>=9; basic evidence (1
   match) keeps l>=6, m>=7, u>=8. Cautious justifications are rewritten
   whenever the LLM fails to cite matching terms.

3. Comparative post-processing: for each criterion, if some options have
   direct evidence and others do not, evidence-bearing options are
   guaranteed to have m at least 1.0 higher than the highest no-evidence
   option — ensuring relative differentiation.

Also includes prior fixes from this session:
- Fixed _SECTION_HEADER_RE regex (.*? for option name) so the guardrail
  correctly parses RAG section headers
- Final decision payload now built from criterion_meta and score data
  (not raw session state) to prevent name-mismatch errors
- Test mocks updated to patch _get_embedding_function, preventing hangs
  from SentenceTransformer model download during tests


After all this work to my llm model is generating same score for both options based on criteria and gives justification that no skills is found even though one resume mentions communication skills 



CURSOR AI PROMPTS

1.
Act as an expert Staff Software Engineer and System Architect. I need to build a "Decision Companion System" as a take-home assignment.

The system must evaluate multiple options against weighted criteria, rank them, and explain the decision. It must not act as a black box; the core decision logic must be deterministic math, while AI is used strictly for data gathering and synthesis.

TECH STACK:
- Framework: Python / Django
- Frontend: CLI (implemented via a Django Custom Management Command)
- AI Orchestration: LangChain & LangGraph
- LLM: Llama-3 (via Groq Cloud API)
- Data Contracts: Pydantic

CORE ALGORITHM:
Weighted Sum Model (WSM). For each option, multiply the criterion score (1-10) by the criterion weight, and sum the results to get the total score.

ARCHITECTURE & DATA FLOW (LangGraph State):
1. User Input Phase (CLI): The user defines the problem, options, and criteria (with 1-10 weights) via terminal inputs.
2. AI Research Node: A LangGraph node takes the inputs and calls the Groq Llama model using `with\_structured\_output` and a strict Pydantic schema. The AI researches each option against each criterion, returning a strict integer score (1-10) and a 1-sentence factual justification.
3. Human-in-the-Loop Phase (CLI): The CLI displays the AI's suggested scores. The user MUST be prompted to either accept the AI's scores or manually overwrite them. This ensures the system is not hard-coded and the user retains control.
4. Calculation Node: A pure Python deterministic function applies the WSM algorithm to the finalized scores and ranks the options.
5. Synthesis Node: A LangGraph node takes the WSM math results (winner, loser, score breakdown) and calls the Groq Llama model to write a short, human-readable summary explaining exactly *why* the winner won based on the math and weights.

DELIVERABLES REQUIRED:
Please write the complete code for this project, including:
1. `models.py`: Django models (if necessary for state) or Pydantic state schemas for LangGraph.
2. `graph.py`: The complete LangGraph workflow defining the nodes, edges, and state.
3. `llm\_services.py`: The LangChain setup using `ChatGroq` and the Pydantic schemas for structured output.
4. `management/commands/decide.py`: The Django custom management command that acts as the interactive CLI, handles the `input()` loops, triggers the LangGraph workflow, and handles the Human-in-the-Loop editing step.

Focus on clean, readable, modular code. Include robust error handling for the CLI inputs and the JSON parsing from the Groq API.

2.

I'm working on a file called decision_comp/llm_services.py for my project. I'm using this library called Pydantic to check my inputs, but my code keeps crashing before the app even starts.

3.

Explain clearly how to set up the virtual environment in python and to run this project step by step ensuring that the project runs without any error, and the contents of requirement.txt which is needed to be downloaded and also where to set up the groq api key in the project folder

4.

I need to refactor the core decision algorithm in this project. We are currently using the Weighted Sum Model (WSM), but because our AI Research Agent is estimating the scores, a crisp 1-10 integer is too brittle and assumes perfect AI accuracy. WSM is also overly compensatory.

To ensure better, more accurate performance and mathematically absorb the AI's uncertainty, we are upgrading the calculation engine to **Fuzzy TOPSIS**.

Please refactor the codebase to implement this. Here are the specific requirements:

1. UPDATE PYDANTIC SCHEMAS (e.g., in `models.py` or `llm\_services.py`):
- Change the AI's output from a single integer score to a Triangular Fuzzy Number (TFN).
- The schema should require a tuple or list of three floats: `(l, m, u)` representing the worst-case, most-likely, and best-case scores (e.g., [7.0, 8.0, 9.0]).

2. UPDATE THE AI RESEARCH PROMPT:
- Update the system prompt sent to the Groq Llama model. Instruct it to output these Triangular Fuzzy Numbers (1-10 scale) instead of a single integer, reflecting its confidence range.

3. REWRITE THE CALCULATION NODE (The core math logic):
Please write a clean, deterministic Python function that executes the Fuzzy TOPSIS steps:
- Step 1: Normalize the fuzzy decision matrix (remember to handle "Benefit" criteria differently than "Cost" criteria if we have them; for now, assume higher is better / Benefit).
- Step 2: Multiply the normalized fuzzy numbers by the user's crisp integer weights.
- Step 3: Identify the Fuzzy Positive Ideal Solution (FPIS) and Fuzzy Negative Ideal Solution (FNIS).
- Step 4: Calculate the distance of each option from the FPIS and FNIS using the Euclidean Vertex Method.
- Step 5: Calculate the final Closeness Coefficient (CC_i) to rank the options.

4. UPDATE THE CLI (Django Management Command):
- Ensure the terminal output formats the AI's fuzzy ranges cleanly for the user during the Human-in-the-Loop review phase.

Please review the existing files and propose the code changes to implement this Fuzzy TOPSIS pipeline.



5.


Clearly explain the error occured and implement a plan to fix it


6.

For Benefit criteria (e.g., Battery): Divide the TFN by the highest  value in that column.For Cost criteria (e.g., Price): Divide the lowest  value in that column by the TFN (this mathematically flips the scale so a lower price becomes a higher score).

is this logic implemented ?

7.

For Benefit criteria (e.g., Battery): Divide the TFN by the highest value in that column.For Cost criteria (e.g., Price): Divide the lowest  value in that column by the TFN (this mathematically flips the scale so a lower price becomes a higher score).

plan and implement this logic

8.

Make the necessary plan to implement the idea of I "the Ai model to decide a wether a particular criteria is cost or benefit criteria and user has to verify and accepts the Ai model's classification of the criteria"


9.

Act as an expert Staff Software Engineer. We are refactoring our "Decision Companion System". We are dropping the Django CLI management command and replacing the frontend with Streamlit to better handle our Human-in-the-Loop requirements and Triangular Fuzzy Numbers (TFNs). Django will now act strictly as a headless API backend.

Please execute the following architectural refactoring:

1. CREATE DJANGO API ENDPOINTS (in `views.py` and `urls.py`):
Create two robust JSON endpoints using standard Django (or Django Ninja/DRF if installed):
- `POST /api/research/`: Accepts a JSON payload of 'options' and 'criteria'. This endpoint triggers our LangGraph workflow (which includes the Researcher Agent and the Critic Agent validation loop). It must return the AI-generated Triangular Fuzzy Numbers (l, m, u) and justifications.
- `POST /api/calculate/`: Accepts the user-approved TFN scores and criteria weights. This endpoint runs the deterministic Python Fuzzy TOPSIS calculation node and the LangGraph Synthesis node. It returns the final ranked list (Closeness Coefficients) and the AI's plain-text explanation of the winner.

2. CREATE THE STREAMLIT FRONTEND (`streamlit\_app.py` in the root directory):
Build a clean, professional Streamlit UI with the following stateful flow:
- Phase 1 (Inputs): Use `st.text\_input` and `st.slider` (for 1-10 weights) to let the user dynamically add multiple Criteria. Use `st.text\_input` to dynamically add multiple Options.
- Phase 2 (Research): A button to trigger a `requests.post` to `/api/research/`. Show `st.spinner("AI Agents researching and validating data...")` while waiting.
- Phase 3 (Human-in-the-Loop): Take the JSON response and convert the fuzzy scores into a Pandas DataFrame. Display this using `st.data\_editor`. The user MUST be able to click into the grid and manually edit the (l, m, u) values if they disagree with the AI.
- Phase 4 (Calculation): A "Run Final Decision" button that sends the edited dataframe state via `requests.post` to `/api/calculate/`.
- Phase 5 (Results): Display the winning option prominently using `st.success` or `st.metric`. Display the AI's explanation text, and render a simple bar chart of the final Closeness Coefficients (CC_i) to show the math visually.

3. CLEANUP:
- Delete the old `management/commands/decide.py` file as we no longer need the CLI.
- Ensure CORS is configured in `settings.py` if Streamlit is running on port 8501 and Django on 8000.

Please review the existing LangGraph state and Fuzzy TOPSIS functions to ensure the data structures align with these new HTTP boundaries, and propose the code changes.


10.

Act as an expert Staff Software Engineer and UX Architect. We need to refactor our Streamlit frontend and the underlying backend logic for our Decision Companion System. The goal is to make the UI dynamic based on user counts, separate the data grids by criteria, and correctly handle the mathematical difference between Cost and Benefit criteria in Fuzzy TOPSIS.

Please implement the following changes across the Streamlit app, Pydantic schemas, LangGraph prompts, and the core math engine:

1. DYNAMIC UI INPUTS (Streamlit Frontend):
- Add two `st.number\_input` fields at the very top: "Number of Options" and "Number of Criteria".
- Based on these counts, dynamically render input rows.
- For Options: Render basic text inputs for the option names.
- For Criteria: Render columns containing 'Criterion Name', 'Weight (1-10)', and a new 'Description (Optional)' text area.
- There should also be a high-level "Overall Goal/Context" text input for the user's primary objective.

2. PYDANTIC SCHEMAS & LLM PROMPTS:
- Pass the user's optional 'Description' for each criterion to the LLM Research Agent so it scores based on the user's exact definition, not just the criterion name.
- The LLM must also classify each criterion. Update the schema so the LLM returns an enum or string classifying the criterion as either a "benefit" (higher score is better) or "cost" (lower score is better).

3. PER-CRITERION DATA EDITORS (Human-in-the-Loop):
- During the Human-in-the-Loop review phase, DO NOT render one giant table.
- Iterate through the criteria and render a separate `st.data\_editor` for EACH criterion.
- Each table should show all the Options as rows, and the (l, m, u) fuzzy scores as editable columns.
- Above or beside each table, include an editable Streamlit widget (like `st.selectbox`) displaying the LLM's guess for "Benefit/Cost" so the user can manually override it if the AI guessed the criterion type incorrectly.

4. FUZZY TOPSIS MATH UPDATE (Cost vs. Benefit Normalization):
- Rewrite the Normalization step in the pure Python calculation node. You MUST apply different formulas based on the criterion type passed from the UI:
  - For BENEFIT criteria: Divide the fuzzy numbers (l, m, u) by the maximum upper bound (u_max) in that criterion's column. Formula: (l/u_max, m/u_max, u/u_max).
  - For COST criteria: Divide the minimum lower bound (l_min) in that criterion's column by the fuzzy numbers, reversing the order. Formula: (l_min/u, l_min/m, l_min/l).

5. TWO-PART EXPLANATION (Synthesis Agent):
- Update the final LangGraph Synthesis Agent prompt. It must output its final explanation in two distinct Markdown sections:
  - Section 1: "Algorithmic Breakdown" - Explains exactly how the winner won mathematically (referencing the weights, closeness coefficients, and distances to the ideal solutions).
  - Section 2: "Contextual Fit" - Explains why this mathematical winner aligns with the user's "Overall Goal" and the specific "Descriptions" they provided for the criteria in step 1.


11.

Act as an expert Staff Software Engineer and UX Architect. We need to implement several major upgrades to our Streamlit + Django/LangGraph Decision Companion System. 

Please implement the following 5 features:

1. RAG IMPLEMENTATION VIA CHROMADB:
- Add a file upload widget `st.file_uploader(type=["pdf"], accept_multiple_files=True)` in the Streamlit frontend.
- In the backend (LangChain/LangGraph logic), implement a document loading and chunking pipeline (e.g., using `PyPDFLoader` and `RecursiveCharacterTextSplitter`).
- Embed the chunks and store them in an ephemeral `Chroma` vector database. 
- Update the AI Research Agent to retrieve context from this ChromaDB based on the options and criteria, and strictly base its fuzzy scoring (l, m, u) on the retrieved PDF data.

2. CLEAR UI EXAMPLES (Placeholder text):
- In the Streamlit `st.text_area` for the "Overall Goal/Description", add a detailed `placeholder` argument so users immediately understand the concept. 
- Example placeholder: "Example: We are evaluating three candidates for a Team Lead role. I have uploaded their resumes as PDFs. Please evaluate them based on Leadership, System Design, and Communication skills."

3. COST/BENEFIT CLASSIFICATION CONFIRMATION (Human-in-the-Loop):
- After the LLM guesses whether each criterion is a "Cost" or "Benefit", pause the workflow.
- Display a sub-header: "Are you satisfied with the criteria classification?"
- Render a Streamlit data editor or a series of dropdowns showing the AI's classification for each criterion, allowing the user to explicitly approve or change the type (Cost/Benefit) before the system proceeds to calculate the scores.

4. SEPARATE ALGORITHM EXPLANATION WEBPAGE (Multipage App):
- Convert the Streamlit app to a multipage setup (e.g., create a `pages/algorithm_explanation.py` file or use `st.switch_page()`).
- Add a prominent button at the end of the main workflow: "View Detailed Mathematical Breakdown".
- On this new page, Section 1 must explain how Fuzzy TOPSIS works in beginner-friendly terms with a small, simple static example. 
- Section 2 must be fully dynamic: Pass the intermediate math state (Normalized Matrix, Weighted Matrix, Positive/Negative Ideals, and Closeness Coefficients) from the actual run via `st.session_state`. Show the step-by-step mathematical calculation of exactly how the winning option won.

5. CUSTOM FOOTER:
- Add a clean, centered footer to the bottom of all Streamlit pages using `st.markdown` with HTML/CSS. 
- The footer must contain exactly these details:
  Sayon Jose
  sionjose2004sj@gmail.com
  7025807399

Please review the current codebase, ensure state is managed correctly via `st.session_state` to handle the multipage data passing, and propose the code changes.



12.



how does the rag setup works in the project now?


13.


Act as an Expert Prompt Engineer and AI Architect. We are refining the System Prompt for the AI Evaluation Agent in our RAG-based Decision Companion System. 

Currently, the LLM is suffering from "Cautious Alignment"—it refuses to give high scores or outputs "not known" if the exact keyword of the criterion isn't explicitly written in the retrieved PDF text. 

Please locate the system prompt used by the LangGraph Research/Evaluation agent and replace it with the following rigorously engineered prompt. This new prompt explicitly commands the AI to infer, deduce, and generously reward concepts that are SIMILAR or RELATED to the criterion.

--- NEW SYSTEM PROMPT ---
You are an Expert Analyst and Strategic Evaluator. Your job is to objectively score specific options based on provided documentation.

You are evaluating the option: '{option_name}'
Against the criterion: '{criterion_name}'
User's Description of this Criterion: '{criterion_description}'

Here is the factual context retrieved from the uploaded documents:
<context>
{rag_context}
</context>

CRITICAL INSTRUCTIONS - AVOID CAUTIOUS ALIGNMENT:
1. INFER AND DEDUCE (NO COP-OUTS): You are strictly FORBIDDEN from outputting "not known", "not mentioned", or "insufficient data". Human-written documents rarely align perfectly with evaluation rubrics. You MUST read between the lines and deduce the score.
2. REWARD SIMILARITY & PROXIES: If the exact criterion is not mentioned, you must actively look for transferable skills, similar features, or proxy metrics. If the text mentions anything semantically related to the criterion, you MUST give it credit and score it accordingly. 
   - Example 1: If evaluating a software for "Security" and it mentions "Bank-grade encryption", score it HIGH (8-10) even if the word "Security" is missing.
   - Example 2: If evaluating a candidate for "Leadership" and they mention "Mentored junior devs" or "Managed project delivery", score them HIGH (7-9). 
3. BENEFIT OF THE DOUBT: If the provided context implies competence or quality in a related area, assume the baseline for this criterion is strong. Use the fuzzy range to express uncertainty (e.g., if you are guessing based on related info, make the gap between l and u wider, like 5, 7, 9).

TRIANGULAR FUZZY SCORING:
Output a Triangular Fuzzy Number (l, m, u) on a scale of 1 to 10. 
- l = Worst-case estimate / Most pessimistic score
- m = Most likely estimate / Most realistic score
- u = Best-case estimate / Most optimistic score

JUSTIFICATION: 
Provide a 1-2 sentence justification explaining exactly which "similar concept" or "proxy metric" from the text you used to deduce this score.

OUTPUT FORMAT:
Return a valid JSON object strictly matching this schema:
{{
  "lower_bound": float,
  "most_likely": float,
  "upper_bound": float,
  "justification": "string"
}}
-------------------------

Please update the code to use this new prompt template. Ensure the JSON parser (e.g., Pydantic output parser or standard json.loads) is configured to safely extract these floats and the justification.



14.


now i need to remove the general pdf upload process along with description in the first phase and add file upload as optional option for each options user provides and generate scores for criterion based on the pdf uploaded and also provide a typo what adding files does to  an option



15.


Act as a Senior AI Architect. Our deterministic guardrails (stem matching and keyword annotation) are failing because they rely on lexical string matching instead of semantic meaning. For example, "Public Relations" proves "Communication", but it doesn't contain the stem "commun", so the guardrail ignores it and the LLM outputs a lazy default score.

Please execute the following refactor to fix the retrieval and evaluation pipeline:

1. REMOVE BRITTLE GUARDRAILS:
- Delete `_annotate_rag_context` and `_criterion_terms`. Lexical string matching defeats the purpose of an LLM. 
- Remove the deterministic score-floor overrides. We want the LLM to do the math, not python `if/else` statements.

2. IMPLEMENT QUERY EXPANSION (The Real Fix):
- When querying ChromaDB for a criterion, DO NOT just search the exact criterion name.
- Create a lightweight LangChain prompt right before the retriever that does "Query Expansion". 
- Ask a fast LLM call: "Generate 5 comma-separated proxy keywords or related concepts for the criterion: '{criterion_name}'. User's description: '{criterion_description}'."
- Pass THAT expanded list of keywords into the ChromaDB retriever. This ensures the vector DB pulls chunks containing "Public Relations" when searching for "Communication".

3. THE "RUTHLESS EVALUATOR" PROMPT OVERHAUL:
- Update the main Evaluation LLM system prompt to be aggressively semantic.
- Add this instruction: "CRITICAL: The context will rarely contain the exact word '{criterion_name}'. You MUST look for semantic proxies. If evaluating 'Communication', roles like 'Public Relations', 'Teamwork', or 'Presentations' are absolute proof of high capability. If you see proxy evidence, you MUST output a high score (7+) and explicitly quote the proxy evidence in your justification."

4. EMPTY CONTEXT CATCHER:
- If `rag_context` is truly empty or purely whitespace after retrieval, do NOT call the LLM for RAG evaluation. Fallback immediately to a baseline prompt telling the LLM: "No documents provided for this option. Infer a baseline score using general knowledge about '{option_name}'."


16.

I need the recommendation to be rank based  and not a single recommendation


17.

what are the contents that is needed to be present in the readme file?

18. 

Create file readme.md and generate the contents required based on the project

